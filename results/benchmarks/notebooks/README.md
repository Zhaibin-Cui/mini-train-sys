# Executed server notebooks

This directory contains compact executed copies of the industrial RTX 4090 kernel benchmark
notebooks and their execution logs. Editable source notebooks remain under `benchmarks/operators/`.

The notebooks are evidence, not the canonical headline source. Stable aggregate metrics and
figures live under `results/benchmarks/`; conclusions live under `reports/engineering/`.
