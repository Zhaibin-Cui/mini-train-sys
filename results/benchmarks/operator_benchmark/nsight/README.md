# Nsight evidence

Selected forward/backward attention profiles retained from kernel development. These are profiler
artifacts, not aggregate benchmark summaries. The canonical dispatch and performance evidence is
under `../resume_summary/`.
